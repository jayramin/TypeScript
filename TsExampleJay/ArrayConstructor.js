var names = new Array("Jigar", "Jay", "Harsh", "Himanshu", "Kailash");
console.log(names[0]); // Jigar
console.log(names.length); // 5
// for('starting',ending,progress) { body } 
for (var i = 0; i < names.length; i++) {
    
    console.log(i);
    console.log(names[i]);
}
